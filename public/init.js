// Intentionally a no-op.
// (v1 used this to show a loading screen and force a full page reload on first
// visit, which added ~3s of delay before anything rendered. The site now loads
// instantly, so no pre-hydration script is needed.)
